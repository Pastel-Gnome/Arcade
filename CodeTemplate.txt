
CODE TEMPLATE:
//This is a single line comment (notes that won't be read as instructions to the computer).
//Different syntax is used for comments depending on the language.

//Title of Game: Minecraft
//Name: Amelia Lewey


if (nightTime == true) {
    findShelter;
} else {
    collectResources;
}

if ((mobAhead == true) && (haveSword == true)) {
    fightMob;
} else {
    runAway;
}

if (health <= 3) {
    panic;
} else {
    eatBread;
}


